document;  // It returns the whole document 
document.getElementById('p1')    // It gives the element by the mentioning id
document.getElementsByClassName('para')  // It gives the element by the classname
